{
  "version": "3.1.0",
  "private": true,
  "packageManager": "pnpm@8.5.1",
  "scripts": {
    "lint": "eslint .",
    "release": "pnpm -r publish --no-git-checks",
    "version": "bumpp",
    "up": "taze major -I",
    "up:pkg": "pnpm run -r --parallel up"
  },
  "devDependencies": {
    "@vividcat/eslint-config": "workspace:*",
    "bumpp": "^9.2.0",
    "eslint": "^8.48.0",
    "taze": "^0.11.2"
  },
  "engines": {
    "node": ">=16.17.0"
  },
  "eslintConfig": {
    "extends": "@vividcat",
    "ignorePatterns": [
      "fixtrue"
    ]
  }
}
